sed -i 's/IconButton(onClick = { navController.navigate("notifikasi") }) {/val hasUnread by NoxNotificationManager.notificationsList.collectAsState()\n                            IconButton(onClick = { navController.navigate("notifikasi") }) {\n                                if (hasUnread.isNotEmpty()) {\n                                    BadgedBox(badge = { Badge { Text(hasUnread.size.toString()) } }) {\n                                        Icon(Icons.Default.Notifications, contentDescription = "Notifikasi", tint = ColorNotification)\n                                    }\n                                } else {/g' app/src/main/java/com/example/NoxCrashApp.kt
sed -i 's/Icon(Icons.Default.Notifications, contentDescription = "Notifikasi", tint = ColorNotification)/Icon(Icons.Default.Notifications, contentDescription = "Notifikasi", tint = ColorNotification)\n                                }/g' app/src/main/java/com/example/NoxCrashApp.kt
